# 模块 7 · 排查简版（单 Agent 直连工具）+ 证据台账 — 版本 C

> 编排理念：🟡在本项目真实代码上改造（把 `definetions.py` 里躺着没接的工具真正接进图；把 M4 的单次工具调用改成 create_agent 的 ReAct 循环）→ 🔴自研证据台账与回流补证。**本实现永久保留为消融 A2（去 subagent 对照组）。**

**本模块的能力边界（先对齐）**
- **入口是 M5 的自建 FastAPI，端口 8000**；checkpointer 已就位，可以从 `runs/checkpoints.db` 看 state。
- **本模块是"6 个工具全部接入"的时点**。当前真实状况必须先认清：
  | 工具 | `definetions.py` 里有定义吗 | 图里 bind 了吗 | 本模块要做什么 |
  |---|---|---|---|
  | `query_metrics` | 有 | **已 bind**（M3 起） | 保持 |
  | `query_logs` | 有 | 否 | **T7.2 接进 tools 列表并真实调一次** |
  | `query_trace` | 有 | 否 | **T7.2 接进 tools 列表并真实调一次** |
  | `query_cmdb` | 有 | 否 | **T7.2 接进 tools 列表并真实调一次** |
  | `query_metrics_raw`（= 计划里的 `raw_promql`） | 有 | 否 | **T7.4 单独接入，走独立预算 + 审计高亮** |
  | `query_logs_raw`（= 计划里的 `raw_logql`） | **没有，定义都要新写** | 否 | **T7.4 新写定义 + 接入** |
- 没有任何一步可以假设"工具都已经能用"——T7.3 的单 Agent 之所以能"持全部只读工具"，是因为 T7.2/T7.4 先把它们一个个接进来并各真实调用过一次。

## 学完本模块你能做到的 4 件事
1. 把 `definetions.py` 里已写好但从没被用过的工具真正接进图，并各自真实调用一次拿到真数据。
2. 用一个持有全部只读工具的单 Agent（ReAct 循环）把一次真实告警查到有结论。
3. 让 Agent 每次取证都把关键结果按格式登记为带编号的证据（EV-M-001…），落盘可回放，结论必引 `[EV-*]`。
4. 把 raw 通道（PromQL/LogQL 逃生舱）作为**受限能力**单独接入：独立预算、审计高亮、prompt 明示"模板不够用才准用"。

## 本模块交付物清单（文件路径级）
- `iris-agent-platform/src/agent/tools/definetions.py`（**改造**：新增 `make_query_logs_raw` 定义）
- `iris-agent-platform/src/agent/tools/registry.py`（**新建**：`make_template_tools(client)` / `make_raw_tools(client)`，一处装配供 M7 单 Agent 与 M10 subagent 共用）
- `iris-agent-platform/src/agent/evidence.py`（**新建**：`EvidenceStore`）
- `iris-agent-platform/src/agent/investigator.py`（**新建**：单 Agent 排查，消融 A2 的正式实现）
- `iris-agent-platform/src/agent/nodes/investigate.py`（**改造**：从 M4 的"一次 query_metrics"换成调单 Agent）
- `iris-agent-platform/src/agent/state.py`（**改造**：新增 `RcaReport`）
- `runs/incidents/{id}/evidence/`、`runs/incidents/{id}/report_draft.md`（运行时产物）

---

## 任务 T7.1 · 证据台账 EvidenceStore

**id**：T7.1 ｜ **所属模块**：M7 ｜ **优先级**：P0

**一句话目标**：写一个能按类型编号、按 §0.6 格式落盘、且复用 M5 防穿越校验的证据台账。

**前置知识（≤2 新概念）**
- **证据台账（可回放取证）**
  1. 每次工具取证，把"来源 + 摘要 + 关键摘录"写成一份带编号的 Markdown 文件。
  2. 结论不再靠 Agent 的记忆，而是靠**引用编号**支撑：报告里写 `[EV-M-001]`，任何人都能翻到那份文件复核。
  3. 这是 M9 反幻觉验收的地基——M9 的确定性预检就是去 `evidence/` 里查这些编号存不存在。
  4. *Java 类比*：结构化的审计日志 + 附件，每条有唯一 id 可溯源。
  5. 📖（自研；格式见 `00-execution-plan.md` §0.6）
- **落盘路径复用 M5 的 `incident_dir()`**
  1. 证据只能写进 `runs/incidents/{id}/evidence/` 子目录。
  2. **不要在这里再写一套路径校验**——M5 的 `paths.incident_dir()` 已经做了"白名单正则 + `resolve()` 前缀比对"两层，直接用它当基目录。
  3. 本模块要新增的是**文件名维度**的校验：`evidence_id` 也是拼进路径的，同样要过白名单正则 `^EV-[MLTS]-\d{3}$`。
  4. *Java 类比*：基目录的安全策略由平台层统一提供，业务层只负责自己那一段文件名的合法性。

### 🟡 第 1 段 · 改造（在 M5 的落盘地基上长出台账）

写 `src/agent/evidence.py`，签名先定死：

```python
class EvidenceStore:
    """一次事故的证据台账，所有证据落在 runs/incidents/{incident_id}/evidence/ 下."""

    def __init__(self, incident_id: str) -> None:
        """基目录取自 M5 的 paths.incident_dir(incident_id)，路径安全由它保证."""

    def new_id(self, kind: Literal["M", "L", "T", "S"]) -> str:
        """按类型分配下一个编号：M=metrics / L=logs / T=trace / S=其他(cmdb/changes)。
        返回形如 'EV-M-001'。序号靠**扫描已有文件名**求最大值 +1，不能用内存计数器——
        进程重启（M5 的 kill-resume 是常态）后内存计数会归零，编号就乱了。"""

    def write(
        self,
        evidence_id: str,
        source: str,        # 取证来源，形如 "query_metrics(template=error_rate,service=pm-question,window=1800)"
        agent_role: str,    # 取证者角色，M7 恒为 "investigate"；M10 起是各 subagent 的角色名
        degraded: bool,     # 网关信封的 degraded 原样带进来，不许省
        truncated: bool,    # 网关信封 meta.truncated 原样带进来，不许省
        summary: str,       # ≤3 句的结论式摘要（"pm-question 的 p99 在 10:12 从 80ms 涨到 2.1s"）
        excerpt: str,       # 关键数据摘录，保留能支撑 summary 的最小片段，不要贴整个响应
    ) -> Path:
        """写一份证据文件并返回路径。文件格式 = YAML frontmatter + 正文，见下。"""

    def list_ids(self) -> list[str]:
        """列出本事故已有的全部证据编号，M9 的确定性预检和 M10 的 lead 摘要工具都用它."""

    def read(self, evidence_id: str) -> str:
        """读一份证据全文。M9 verify 的 LLM 段靠它拿到证据原文."""
```

证据文件格式（§0.6）：

```markdown
---
evidence_id: EV-M-001
source: query_metrics(template=p99,service=pm-question,window=1800)
agent_role: investigate
ts: 2026-08-05T10:14:22Z
degraded: false
truncated: false
---

pm-question 的 p99 时延在 10:12 从 ~80ms 阶跃到 ~2.1s，之后维持高位未回落。

| ts | p99_ms |
|---|---|
| 10:10 | 82 |
| 10:12 | 2143 |
| 10:14 | 2088 |
```

### 🔴 第 2 段 · 自研（编号与文件名的安全校验）

**从空文件**写校验：
1. `evidence_id` 必须匹配 `^EV-[MLTS]-\d{3}$`，否则抛 `ValueError`——它会被拼进文件名，属于路径的一部分；
2. 最终写入路径 `resolve()` 后必须仍在 `incident_dir(incident_id)` 之内（M5 的基目录校验挡住了 incident_id，这一层挡住 evidence_id）；
3. 自测三种攻击都被拒：`../../etc/passwd`、绝对路径 `/tmp/x`、指向外部的软链。

**落笔顺序**
1. 写 `EvidenceStore.__init__` + `new_id`，先只做编号，不落盘。
2. 连续 `new_id("M")` 两次，确认拿到 `EV-M-001`/`EV-M-002`；再 `new_id("L")` 确认是 `EV-L-001`（按 kind 分别递增，别混）。
3. 写 `write`，落一份带 frontmatter 的文件，肉眼核对 6 个 frontmatter 字段齐全。
4. **重启进程再 `new_id("M")`**，确认拿到的是 `EV-M-003` 而不是 `EV-M-001`（证明是扫文件不是内存计数）。
5. 自研校验：用 `EV-M-../x`、`/tmp/x`、软链三种输入各试一次，确认都抛异常且没落盘。
6. 写 `list_ids`/`read`，供 M9 使用。

**关键提示（≤3）**
- 编号按 kind 分别递增（M/L/T/S），别用一个全局计数器。
- `new_id` 扫文件而不是内存计数——M5 的 kill-resume 会让内存计数器归零。
- 这个类是 M9（预检查编号）/M10（subagent 共用台账）的共同依赖，**格式即契约**，先定死再往下走。

**卡住降级路径（30 分钟没思路）**
- 编号乱：确认 `new_id` 是扫已有文件名求最大值，且扫的是本 incident 目录不是全局。
- 校验没生效：确认比较的是 `Path.resolve()` 之后的对象前缀关系，不是字符串 `startswith`（软链靠字符串比对挡不住）。
- 视频：证据台账无对口视频，以自研为主。
- 问 AI：「Python 里我要保证写文件路径必须落在某个基目录内、防 `..` 穿越和软链逃逸，正确的校验写法是什么？给校验函数 + 3 个攻击用例。」

**真实验收（含异常路径）**
```bash
cd iris-agent-platform
# 本任务偏库，用 REPL 直接驱动真实类（不写一次性脚本文件）
uv run python -c "
from agent.evidence import EvidenceStore
s = EvidenceStore('inc-20260805-001')
print(s.new_id('M'), s.new_id('M'), s.new_id('L'))
"
# 期望：EV-M-001 EV-M-002 EV-L-001

ls runs/incidents/inc-20260805-001/evidence/
head -12 runs/incidents/inc-20260805-001/evidence/EV-M-001.md   # 6 个 frontmatter 字段齐全

# 异常路径：三种穿越都要被拒且不落盘
uv run python -c "
from agent.evidence import EvidenceStore
s = EvidenceStore('inc-20260805-001')
for bad in ['EV-M-../../x', '/tmp/x', 'EV-M-001/../../../etc/passwd']:
    try: s.write(bad, source='x', agent_role='investigate', degraded=False, truncated=False, summary='x', excerpt='x')
    except Exception as e: print('rejected:', bad, type(e).__name__)
"
```
判定特征：EV 编号按类型递增且重启后不回退；frontmatter 6 字段齐全；三种穿越均被拒。依赖真实外部服务：无（纯本地，但 T7.3 起证据内容全部来自真实工具调用）。

**完成判定**
- [ ] EV 编号按 kind 递增，且**重启进程后不回退**（扫文件而非内存计数）
- [ ] 落盘格式合 §0.6，frontmatter 含 `evidence_id/source/agent_role/ts/degraded/truncated` 六字段
- [ ] 基目录复用 M5 的 `incident_dir()`，没有重复实现一套路径校验
- [ ] `evidence_id` 白名单正则生效，`..`/绝对路径/软链三种穿越均被拒
- [ ] `list_ids`/`read` 可用（M9 的依赖）

---

## 任务 T7.2 · 接入剩余 4 个模板工具

**id**：T7.2 ｜ **所属模块**：M7 ｜ **优先级**：P0

**一句话目标**：把 `query_logs`/`query_trace`/`query_cmdb` 从"定义写好了但没人用"变成"图里真实调用过"。

**前置知识（≤1 新概念）**
- **工具装配与"被定义"≠"被接入"**
  1. `@tool` 装饰器只是把函数变成一个可被模型调用的对象；模型能不能看见它，取决于有没有出现在 `bind_tools([...])` 或 `create_agent(tools=[...])` 的列表里。
  2. 当前 `definetions.py` 里有 5 个 factory，但 `graph.py` 只 bind 了 `make_query_metrics` 一个——其余 4 个从没被真实调用过，**参数契约有没有对上 M2 网关是未经验证的**。
  3. 所以本任务不是"改个列表"，而是"逐个真实调一次，确认返回真数据、字段名对得上"。
  4. *Java 类比*：写了 Feign 接口不等于联调过；上线前每个接口都得真打一次。

### 🟡 第 1 段 · 改造（逐个接入 + 逐个真打）

1. **新建 `tools/registry.py`**，把"给一个 GatewayClient、返回一组工具"这件事集中到一处（M10 的 subagent 要按域挑工具，靠这里分组）：

```python
def make_template_tools(client: GatewayClient) -> list[BaseTool]:
    """4 个模板通道工具，走网关 per-incident 模板预算（上限 40）."""
    return [
        make_query_metrics(client),
        make_query_logs(client),
        make_query_trace(client),
        make_query_cmdb(client),
    ]


def make_raw_tools(client: GatewayClient) -> list[BaseTool]:
    """2 个 raw 通道工具，走独立预算（上限 5）。T7.4 才接进 Agent，这里先留位."""
    return [make_query_metrics_raw(client), make_query_logs_raw(client)]
```

2. **逐个真打**：在 REPL 里给每个工具各调一次真实网关，核对三件事——① HTTP 通了；② 返回信封 `ok=true` 且 `data` 有真实数据；③ 你写的 Pydantic 字段名和网关实际接收的驼峰字段对得上（现有代码用的是手写映射，如 `templateKey`/`endOffsetSeconds`，写错了不会报错、只会拿到空数据）。
3. **接进图**：把 `nodes/investigate.py` 里 M4 那句 `bind_tools([query_metrics])` 换成 `bind_tools(make_template_tools(client))`。

### 🔴 第 2 段 · 自研（工具级预算计数接线）

M4 写的 `guard.charge_tool_call(ledger, envelope, raw=False)` 到现在为止只被 `query_metrics` 一条路径调用。本任务要把它接到**每一个模板工具的返回路径**上：
1. 统一在 `registry.py` 里包一层，而不是在 5 个 factory 里各抄一遍；
2. 每次调用后 `charge_tool_call(...)` 累加 `tool_calls_used`，并用信封 `meta.tool_calls_remaining` 校准；
3. `check_budget` 在每次工具调用**之前**执行——这一条 M4 已经定死，这里只是把它覆盖到新接入的 4 个工具上。

**落笔顺序**
1. 建 `registry.py`，放 M3 已有的 4 个 factory，跑一次确认没接坏。
2. REPL 里逐个真打 `query_logs`/`query_trace`/`query_cmdb`，把返回信封贴出来核对字段。
3. 把 4 个工具接进 `investigate` 节点的 `bind_tools`。
4. 接 guard 计数，连打若干次看 `tool_calls_used` 与网关的 `tool_calls_remaining` 是否一致。

**关键提示（≤3）**
- `query_trace` 的 `trace_id` 不能凭空造：它来自 `query_logs` 返回里的 `distinctTraceIds`，验收时要按这个顺序串起来调（真实链路是 logs → trace）。
- Pydantic 字段名（snake_case）和网关入参（camelCase）的映射是手写的，写错不报错只返回空数据——每个工具第一次调用必须肉眼核对返回体。
- `query_cmdb` 应该是**第一个**被调的：service 名对不上，后面 metrics/logs/trace 全是空。

**卡住降级路径（30 分钟没思路）**
- 工具返回空 data 但 `ok=true`：八成是字段名映射错了或 service 名不在 CMDB，先用 `query_cmdb` 确认服务名。
- 网关 400：把 `GatewayRequestError` 里带出来的 `body` 打出来，M2 的信封会写明是哪个参数不合法。
- 视频：video-2 P12-16「智能体中定义工具」。
- 问 AI：「我用 `@tool(args_schema=...)` 包了一个 HTTP 工具，调用返回 ok=true 但 data 是空的，我的 args_schema 和网关入参契约这样（贴出），怎么排查是不是字段映射错了？」

**真实 HTTP 验收（含异常路径）**
```bash
cd iris-agent-platform
# 逐个真打，确认返回真数据（不是 mock、不是空 data）
uv run python -c "
from agent.config import AgentRole
from agent.tools.GatewayClient import GatewayClient
from agent.tools.registry import make_template_tools
c = GatewayClient(incident_id='inc-t7-probe', agent_role=AgentRole.INVESTIGATE)
tools = {t.name: t for t in make_template_tools(c)}
print(tools['query_cmdb'].invoke({'service': '<pm-svc>'}))
logs = tools['query_logs'].invoke({'service': '<pm-svc>', 'window': 1800})
print(logs)
tid = logs['data']['distinctTraceIds'][0]              # trace_id 来自 logs，不是编的
print(tools['query_trace'].invoke({'trace_id': tid}))
"
# 期望：4 个工具（含 query_metrics）各返回 ok=true 且 data 非空

# 异常路径：给一个 CMDB 里不存在的 service → 网关 400，抛 GatewayRequestError 且信息可读
```
判定特征：4 个模板工具各真实调用一次拿到真数据；trace_id 来自 logs 的真实返回；不存在的 service 报可读错误。依赖真实服务：M2 网关、Prometheus/Loki/Tempo/Nacos。

**完成判定**
- [ ] `registry.make_template_tools` 返回 4 个工具，`investigate` 节点已 `bind_tools` 全部 4 个
- [ ] 4 个工具各**真实调用过一次**并拿到非空 data（有返回体为证）
- [ ] `query_trace` 的 trace_id 取自 `query_logs` 的 `distinctTraceIds`
- [ ] `guard.check_budget` / `charge_tool_call` 覆盖到全部 4 个工具，本地计数与网关 `tool_calls_remaining` 一致

---

## 任务 T7.3 · 单 Agent 排查 simple_investigator

**id**：T7.3 ｜ **所属模块**：M7 ｜ **优先级**：P0

**一句话目标**：把 M4 那个"调一次 query_metrics 就结束"的节点，换成 `create_agent` 的 ReAct 循环，持全部模板工具取证、登记证据、输出带引用的 `RcaReport`。

**前置知识（≤2 新概念）**
- **`create_agent` + ReAct 循环**
  1. 给 Agent 一组工具和一个目标，它自己决定调哪个、看结果、再调，直到能给结论。
  2. `response_format=RcaReport` 让它最后一步产出结构化对象而不是自由文本。
  3. 用官方 `create_agent`，**别手写循环**，也别用已弃用的 `create_react_agent`。
  4. 对比 M4：M4 是"模型选一次工具 → 我们手动调一次 → 结束"，现在是"模型自己循环到收敛"——所以 M4 那个调用粒度的 guard 从这里开始才真正派上用场（H6）。
  5. 📖 https://docs.langchain.com/oss/python/langchain/agents （create_agent，需核实锚点）· video-2 P10-16「创建 Agent·定义工具」
- **证据登记做成工具，而不是事后解析自由文本**
  1. 给 Agent 一个 `record_evidence` 工具，让它主动登记；比从它的输出里正则抠可靠得多。
  2. `record_evidence` 内部调 T7.1 的 `EvidenceStore.write`，返回分配到的 `EV-*` 编号给 Agent，Agent 之后在结论里引用它。
  3. *Java 类比*：让调用方显式调审计接口，而不是从日志里正则抠。

### 🟡 第 1 段 · 改造（把节点换成 Agent）

先在 `state.py` 里补上本模块负责的模型：

```python
class RcaReport(BaseModel):
    """单 Agent（或 M10 的 lead）产出的根因分析结论。M7 产出，M8 只渲染不重定义."""

    root_service: str        # 根因服务名，必须是 CMDB 里的真实服务名；M12 判分的 acc@service 比对它
    root_cause: str          # 根因分类，用短语而非长句（如 "slow_query" / "threadpool_exhausted"）；判分的 acc@class 比对它
    root_detail: str         # 根因细节：具体是哪条 SQL / 哪个池 / 哪次配置变更
    confidence: Literal["high", "medium", "low"]  # 置信度；M8 在验收 fail 或预算耗尽时会强制降为 low
    causal_chain: list[str]  # 因果链，从根因到告警现象的有序步骤，每步一句话
    evidence_ids: list[str]  # 本结论引用的全部证据编号，必须都能在 evidence/ 里找到（M9 预检就查这个）
    summary_md: str          # 给人读的摘要 Markdown；**只有这个字段允许 M8 的 LLM 润色**
    remediation: str | None = None  # 修复建议；M12 的审批与执行以它为输入，慢查询类只准建议人工处理
```

再写 `src/agent/investigator.py`：

```python
def build_simple_investigator(model, tools, evidence_store, verifier_feedback):
    """组装消融 A2 的单 Agent：create_agent(model, tools + [record_evidence], response_format=RcaReport)."""
```

system prompt 的要点，分点写进去：
1. **先建立地基**：先 `query_cmdb` 确认服务与拓扑，然后才是 metrics/logs/trace；
2. **每次取证后必须调 `record_evidence` 登记**，拿到 `EV-*` 编号；
3. **结论里的每一条断言都要带 `[EV-*]` 引用**，没有证据支撑的推测要显式标成"未验证假设"；
4. 工具返回 `degraded=true` 或 `truncated=true` 时**如实反映**，不许假装数据齐全；
5. 链路顺序提示：trace_id 从 `query_logs` 的 `distinctTraceIds` 里取，不要凭空构造。

`nodes/investigate.py` 改造：
- 建 `EvidenceStore(incident_id)` 和 `GatewayClient(incident_id, agent_role="investigate")`；
- `tools = make_template_tools(client)`（T7.2 接入的 4 个）；
- 跑 Agent，把 `RcaReport` 落盘成 `runs/incidents/{id}/report_draft.md`，state 里只写 `draft_report_ref` 路径（M4 的引用式字段约定）；
- `investigate_rounds` 仍在这里 +1（M4 定的，路由函数不改 state）。

### 🔴 第 2 段 · 自研（回流补证 + token 计量）

**从空文件**写回流分支：
1. `state["verifier_feedback"]` 非空时，把驳回意见拼进 prompt，并显式声明三件事：
   - 已有的证据编号清单（`evidence_store.list_ids()`）；
   - "**增量补证**，不要重头把已有的证据再取一遍"；
   - "复用已有 `EV-*` 编号，只为新取的证据申请新编号"。
2. Agent 每轮结束后从 `usage_metadata` 读 token 累加进 `budget.tokens_used`（复用 M4 的 `guard.charge_tokens`）——ReAct 循环里 LLM 会被调很多次，这里是 token 真正开始烧的地方。
3. `subagents_enabled` 开关预留：`nodes/investigate.py` 里留一个分发点，`False` 走本模块的单 Agent（A2），`True` 留给 M10 的 lead。

**落笔顺序**
1. 先用 T7.2 的 4 个工具 + 一个 `create_agent`，system prompt 只写"查清楚这个服务为什么告警"，跑一次看它会怎么调工具、什么时候停。
2. 加 `response_format=RcaReport`，确认最后一步能产出结构化对象。
3. 加 `record_evidence` 工具，跑一次看 `evidence/` 目录里有没有文件。
4. 把 prompt 的 5 条要点补全，再跑一次，检查结论是否条条带 `[EV-*]`。
5. 自研回流：手工往 state 塞一条 `verifier_feedback`（此时 M9 还没写，但 M4 的字段和回流边已经在了，回流是**真实图行为**不是伪造），确认第二轮是增量补证而不是重头再来。
6. 接 token 计量，跑一次看 `budget.tokens_used` 是否随轮数增长。

**关键提示（≤3）**
- 用官方 `create_agent`，别手写 Agent 循环，也别用已弃用的 `create_react_agent`。
- `record_evidence` 是工具不是解析——如果 Agent 老不调它，问题多半在工具 description 而不在 system prompt。
- 这是消融 A2 的正式配置，M10 上线多 Agent 后靠 `subagents_enabled` 切换，**别删**。

**卡住降级路径（30 分钟没思路）**
- Agent 一个工具都不调：先确认 `args_schema` 的 `description` 写得让模型看得懂（M3/T7.2 定义的），再确认 tools 真的传进了 `create_agent`。
- Agent 无限循环：确认 M4 的 guard 真的在每次调用前检查（H6 说的就是这个场景），以及 `create_agent` 的最大步数上限设了。
- 结论不带引用：把"每条断言必须带 [EV-*]"从 prompt 中段挪到**结论格式要求**那一节，并给一个正例。
- 视频：video-1 P51-55「Agent 概述·调用·绑定工具」；video-2 P14「工具测试和 Agent 整合」。
- 问 AI：「LangChain `create_agent` 给了工具但模型总不调 `record_evidence` 登记证据，我的 system prompt 和工具 description 这样（贴出），怎么让它每次取证后必登记？」

**真实 HTTP 验收（含异常路径）**
```bash
cd iris-agent-platform
uv run uvicorn agent.app:app --port 8000

# 注入某个真实故障 → webhook → 看 investigate 产出
curl -s -XPOST localhost:8000/webhook/alertmanager -d @some-fault-alert.json
ls runs/incidents/inc-*/evidence/                 # 若干 EV-*.md
cat runs/incidents/inc-*/report_draft.md          # 结论条条带 [EV-*]
sqlite3 runs/incidents.db "select status from incident order by created_at desc limit 1;"

# 异常路径 ①：docker stop loki → query_logs 返回 degraded=true
#   期望：证据文件 frontmatter degraded: true，且 Agent 在结论里注明"日志侧数据不可用"
# 异常路径 ②：让某次查询命中大量数据触发 meta.truncated
#   期望：证据 frontmatter truncated: true，且 Agent 引用它下结论时注明"数据不完整，建议缩小 window 复核"
```
判定特征：真实注入 → 生成证据 + report_draft，结论带引用；degraded/truncated 均被如实标注而不是被吞掉。依赖真实服务：M2 网关、Prometheus/Loki/Tempo、investigate 模型。

**完成判定**
- [ ] `RcaReport` 8 个字段定义在 `state.py`（M8 只渲染不重定义）
- [ ] 单 Agent 持 T7.2 接入的全部 4 个模板工具 + `record_evidence`，真实取证并登记 EV
- [ ] `report_draft.md` 落盘，state 里只存 `draft_report_ref` 路径
- [ ] 结论条条带 `[EV-*]`，`RcaReport.evidence_ids` 与 `evidence/` 里的文件一一对得上
- [ ] 回流时增量补证、复用已有 evidence_id，不重头再来
- [ ] `budget.tokens_used` 随 ReAct 轮数增长（M4 的 guard 真的接上了）
- [ ] `subagents_enabled` 分发点预留（A2 配置），本实现不删

---

## 任务 T7.4 · raw 通道接入（受限逃生舱）

**id**：T7.4 ｜ **所属模块**：M7 ｜ **优先级**：P1

**一句话目标**：把 `query_metrics_raw` / `query_logs_raw` 作为**独立预算 + 独立审计**的受限能力接进 Agent，而不是和 4 个模板工具混在一起。

**前置知识（≤1 新概念）**
- **raw 通道为什么要单独对待**
  1. **能力不同**：模板工具的表达式是网关写死的，raw 通道让模型自己写 PromQL/LogQL——表达力大得多，出事的方式也多得多。
  2. **预算不同**：网关侧模板通道 40 次、raw 通道 5 次，是两个独立计数器（M2 契约）；M4 的 `BudgetLedger` 也是分开的 `tool_calls_used` / `raw_calls_used`。
  3. **护栏不同**：raw 查询必须含至少一个 label selector，否则网关直接 403 `NOT_WHITELISTED`（防无范围全表扫）。
  4. **审计不同**：网关 audit_log 的 `template_or_raw` 字段标 raw，用于事后高亮"这次诊断里模型自己写了什么查询"。
  5. 📖（M2 `docs/tool-contract.md` 的 raw 章节；`00-execution-plan.md` §M2）

### 🟡 第 1 段 · 改造（新写 logql raw + 一起接入）

1. **新写 `make_query_logs_raw`**（`definetions.py` 里目前只有 `make_query_metrics_raw`，logql 侧完全没有）。照 `QueryMetricsRawInput` 的同一套写法：
   - `query: str`——原始 LogQL，`description` 里必须写明"表达式中必须含至少一个 label selector，否则网关 403"；
   - `service` / `window` / `end_offset_seconds` 三个参数与 `query_metrics_raw` 保持一致的语义；
   - docstring 明确定位：**模板工具 `query_logs` 覆盖不了时才用**。
   > 命名说明：`00-execution-plan.md` 里叫 `raw_promql`/`raw_logql`，真实代码里 M3 已经把前者写成了 `query_metrics_raw`，为保持一致本项目统一用 `query_metrics_raw`/`query_logs_raw`。
2. `registry.make_raw_tools(client)` 返回这两个，**和 `make_template_tools` 分开两个函数**（M10 的 subagent 要按域挑，`logs-investigator` 拿 `query_logs + query_logs_raw`，`metrics-investigator` 只拿 `query_metrics`）。
3. Agent 的 tools 列表改成 `make_template_tools(c) + make_raw_tools(c)`，并在 system prompt 里单独加一节：
   - "raw 通道全事故只有 5 次，模板工具能表达的一律走模板"；
   - "写 raw 查询时必须带 label selector（如 `{service="pm-question"}`）"；
   - "用了 raw 通道的证据，`source` 字段要原样记录你写的表达式"。

### 🔴 第 2 段 · 自研（独立预算计数 + 审计核对）

1. raw 工具的返回路径上调 `guard.charge_tool_call(ledger, envelope, raw=True)`——走 `raw_calls_used` 而不是 `tool_calls_used`（M4 已经把字段分开了，这里是它第一次真正被用到）；
2. `check_budget` 里 raw 维度触顶只应该拦 raw 工具，**不能把模板工具一起拦掉**——写一个按通道分别判断的分支，并真实验证：连打 6 次 raw 后模板工具仍然可用；
3. 事后核对网关审计：`select template_or_raw, count(*) from audit_log where incident_id=? group by 1`，raw 的条数应与本地 `raw_calls_used` 一致。

**落笔顺序**
1. 新写 `make_query_logs_raw` 的 args_schema + `@tool`。
2. REPL 里各真打一次两个 raw 工具（带 label selector），确认返回真数据。
3. 故意打一条**不带 label selector** 的查询，确认网关 403 且错误可读。
4. 接进 Agent 的 tools 列表，补 system prompt 的 raw 那一节。
5. 接 `charge_tool_call(raw=True)`，连打 6 次验证第 6 次被拦、且模板工具此时仍可用。
6. 跑一次完整事故，核对网关审计里 raw 条数与本地计数一致。

**关键提示（≤3）**
- raw 的预算和模板的预算是**两个计数器**，别共用一个；拦截也要分开拦，否则 raw 用超了会把整轮诊断废掉。
- 网关的 403 `NOT_WHITELISTED` 是硬护栏，但 prompt 里也要写清规则——让模型第一次就写对，比让它撞墙再重试省预算。
- 用了 raw 的证据，`source` 里要留下模型写的原始表达式，这是事后审计"它到底查了什么"的唯一线索。

**卡住降级路径（30 分钟没思路）**
- raw 一直 403：把表达式打出来看有没有 label selector；网关的校验是静态的，`{}` 空 selector 也算没有。
- raw 预算把模板一起拦了：`check_budget` 需要知道"这次要走哪个通道"，把通道作为参数传进去，别只看 ledger。
- 视频：无对口视频，以 M2 的网关契约文档为主。
- 问 AI：「我有两个独立预算的工具通道（模板 40 次 / raw 5 次），怎么设计一个 guard 让 raw 用尽时只拦 raw、不影响模板通道？给结构。」

**真实 HTTP 验收（含异常路径）**
```bash
cd iris-agent-platform
uv run python -c "
from agent.config import AgentRole
from agent.tools.GatewayClient import GatewayClient
from agent.tools.registry import make_raw_tools
c = GatewayClient(incident_id='inc-t74-probe', agent_role=AgentRole.INVESTIGATE)
raw = {t.name: t for t in make_raw_tools(c)}
print(raw['query_metrics_raw'].invoke({'query': 'rate(http_server_requests_seconds_count{service=\"<pm-svc>\"}[5m])', 'service': '<pm-svc>', 'window': 1800}))
print(raw['query_logs_raw'].invoke({'query': '{service=\"<pm-svc>\"} |= \"ERROR\"', 'service': '<pm-svc>', 'window': 1800}))
"
# 期望：两个 raw 工具各返回 ok=true 且 data 非空

# 异常路径 ①：不带 label selector → 网关 403 NOT_WHITELISTED，错误信息可读
# 异常路径 ②：同一 incident 连打 6 次 raw → 第 6 次被本地 guard 或网关 429 拦住，
#             但此时模板工具（query_metrics）仍可正常调用

# 跑一次完整事故后核对网关审计
sqlite3 tool-gateway/data/*.db \
  "select template_or_raw, count(*) from audit_log where incident_id='inc-xxx' group by 1;"
# 期望：raw 那一行的条数 == 本地 budget.raw_calls_used
```
判定特征：两个 raw 工具真实可用；无 selector 被 403；raw 预算独立、用尽不影响模板通道；网关审计能高亮 raw 调用。依赖真实服务：M2 网关、Prometheus、Loki。

**完成判定**
- [ ] `make_query_logs_raw` 新写完成，两个 raw 工具各真实调用一次拿到真数据
- [ ] `registry` 把 raw 与模板分成两个装配函数（M10 的最小权限 subagent 依赖这个分组）
- [ ] system prompt 有独立的 raw 使用规则一节（次数上限 + label selector + source 记录表达式）
- [ ] `charge_tool_call(raw=True)` 走 `raw_calls_used`，与模板计数分开
- [ ] raw 预算用尽只拦 raw，模板工具仍可用（实测验证过）
- [ ] 网关审计 `template_or_raw='raw'` 的条数与本地 `raw_calls_used` 一致
- [ ] **至此 6 个工具全部接入并各真实调用过一次**——后续模块可以放心引用

---
*版本 C 特征：排查全靠"在真实工程上把躺着没接的工具逐个接进来真打一遍 → 改造成 create_agent 的 ReAct 循环 → 自研台账、回流补证与 raw 通道护栏"，第一个能自己取证的 Agent 从本项目已有的代码长出来，不是从官方样例抄来的。*
