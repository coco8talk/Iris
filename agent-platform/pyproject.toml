[project]
name = "agent-platform"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-openai",
    "langchain-openrouter",
    "langchain_core",
    "pydantic>=2.13.4",
    "pydantic-settings",
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
